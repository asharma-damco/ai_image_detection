[FIX] 2026-05-05 — DIRE eager SDXL load caused OOM/shutdown on CPU; deferred to lazy-load + CPU guard added — ai_image_detection/detectors/dire.py:58-89,344-384
[FIX] 2026-05-05 — DDIMInverseScheduler config warning (interpolation_type, use_karras_sigmas, skip_prk_steps) eliminated by filtering unsupported keys before scheduler construction — ai_image_detection/detectors/dire.py:85-87
[FIX] 2026-05-05 — yolo_damage signal in run_custom_pipeline called .get("score") on a list (detect() returns list[dict]); fixed score derivation to max confidence; stored detections+composite_roi+expanded_roi in details — ai_image_detection/pipelines.py:371-395
[ADD] 2026-05-05 — yolo_damage added to run_vehicle_damage_pipeline (was missing entirely); YOLO runs first so composite ROI is available for subsequent detectors; details dict returned — ai_image_detection/pipelines.py:177-245
[ADD] 2026-05-05 — YOLO bounding box + ROI visualisation added to app.py: _draw_damage_boxes() (orange boxes, confidence badges), _draw_roi_overlays() (orange composite + cyan expanded 1.4x ROI), _cluster_detections() (union-find clustering), "Vehicle Damage Detection" expander with per-cluster crop view — app.py:17-110,247-318
[ADD] 2026-05-05 — use_damage_roi option added: when enabled all non-YOLO fraud signals run on the 1.4x expanded damage ROI crop; YOLO still scores full image; sidebar checkbox appears only when yolo_damage signal is selected — ai_image_detection/pipelines.py:261-310,177-220, app.py sidebar + run call
[FIX] 2026-05-05 — logic bug: yolo_damage score (max YOLO confidence) was weighted 0.10 in fraud verdict, causing high-damage vehicles to score as more fraudulent; removed from signals{} in both pipelines and from VEHICLE_DAMAGE_WEIGHTS; redistributed 0.10 across trufor/dual_branch/clip_ufd/rigid/srm; YOLO now drives ROI only — ai_image_detection/config.py:62-70, ai_image_detection/pipelines.py (both pipeline functions + weights dict + SIGNAL_LABELS)
[ADD] 2026-05-05 — manual ROI selection added for all pipeline types (id_card, document_fraud, vehicle_damage, custom): click-and-drag canvas via streamlit-image-coordinates + slider fine-tune expander; blue ROI preview drawn on image; "Run on ROI" button crops image to selection before all signals run; manual_roi param added to run_custom_pipeline; manual_roi_applied flag + banner in results — requirements.txt, ai_image_detection/pipelines.py:274-320, app.py helpers + ROI section + button row
